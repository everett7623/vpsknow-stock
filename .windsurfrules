# Windsurf Rules for VPSKnow Stock

> 完整开发指南见 `CLAUDE.md`

## 项目信息

**VPSKnow Stock** - VPS 实时补货监控 + LowEndTalk 优惠聚合平台

- 技术栈: TypeScript + Next.js 15 + PostgreSQL + Redis + BullMQ
- Monorepo: Turborepo + pnpm workspaces
- Node.js: >=22

## 快速开始

```bash
pnpm install
pnpm dev        # 启动 web + worker + bot
```

## 核心命令

| 命令 | 说明 |
|------|------|
| `pnpm dev` | 开发模式（所有服务） |
| `pnpm build` | 构建所有应用 |
| `pnpm test` | 运行测试 |
| `pnpm typecheck` | TypeScript 类型检查 |
| `pnpm lint` | ESLint 检查 |
| `pnpm format` | Prettier 格式化 |

### 数据库

```bash
cd packages/database
pnpm db:generate   # 生成 Prisma Client
pnpm db:push       # 推送 schema（开发）
pnpm db:migrate    # 创建 migration
pnpm db:seed       # 填充种子数据
pnpm studio        # 打开 Prisma Studio
```

## 架构概览

### 服务结构

```
apps/
├── web/          # Next.js 公开网站（stock.vpsknow.com）
├── worker/       # 库存监控 + LET 抓取（BullMQ）
└── bot/          # Telegram 订阅机器人

packages/
├── database/     # Prisma schema（所有服务依赖）
├── providers/    # Provider 适配器（registry 模式）
├── parsers/      # LowEndTalk 解析器
├── telegram/     # Telegram 工具函数
├── shared/       # 共享类型和常量
└── config/       # ESLint & TypeScript 配置
```

### Provider Adapter

所有 provider 实现 `ProviderAdapter` 接口：

```typescript
interface ProviderAdapter {
  slug: string;
  name: string;
  check(): Promise<StockResult[]>;
}
```

**添加新 provider 步骤：**
1. `packages/providers/src/adapters/newprovider.ts` - 创建适配器
2. `packages/providers/src/registry.ts` - 注册导出
3. `packages/database/prisma/seed.ts` - 添加种子数据
4. `apps/worker/src/index.ts` - 配置 `PROVIDER_INTERVALS`
5. 添加测试文件

### Stock Check 流程

```
Worker 调度任务 → Adapter 抓取 → Engine 处理 → 连续确认 → 推送通知
```

- **连续确认机制**: 需 2 次连续 in-stock 才触发补货通知
- **冷却期**: 防止重复通知同一产品
- **健康检查**: 连续失败会自动暂停 provider

## 开发规范

- **TypeScript**: strict mode，避免 `any`
- **测试**: Vitest，集成测试用真实数据库
- **提交**: Conventional Commits 格式
- **代码风格**: 遵循 ESLint 配置

## 重要约束

- Provider 检查间隔: S-Tier (90-300s) / A-Tier (180s) / B-Tier (300s)
- Telegram 限流: ~20 条消息/分钟
- Stock check 保留天数: 可配置（见 `maintenance.ts`）
- Provider 健康阈值: 连续失败达标会暂停

---

**详细文档**: `CLAUDE.md` 包含完整的数据流、生命周期、部署指南等信息。
